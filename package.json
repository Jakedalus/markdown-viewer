{
  "name": "markdown",
  "version": "1.0.0",
  "description": "Markdown viewer made with React",
  "main": "index.js",
  "dependencies": {
    "marked": "^0.3.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-react": "^6.24.1",
    "live-server": "^1.2.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch",
    "serve": "live-server public --port=3030"
  },
  "author": "Jake Carpenter",
  "license": "ISC"
}
